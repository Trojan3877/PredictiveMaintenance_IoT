name: Trivy Scan

on:
  push:
    branches: [main]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t predictive-maintenance .
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: predictive-maintenance
